# AI Flutter Office — Codex CLI configuration
# See docs/ai-office/runtime-adapters.md for details.

[agents]
max_threads = 3
max_depth = 2
job_max_runtime_seconds = 600

[mcp_servers.dart]
command = "fvm"
args = ["dart", "mcp-server", "--force-roots-fallback"]
